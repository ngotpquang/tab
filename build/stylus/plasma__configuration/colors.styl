// Import all semantic colors that used throughout the whole application (hover, focus, disabled, select, ... colors)
// as well as used components of the application there (input, main menu, tab menu, ...).
